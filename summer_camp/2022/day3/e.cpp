#include <bits/stdc++.h>
using namespace std;

const int N = 1e5+5;

int a[N], b[N], n;


int main(){
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	int aux = 1;
	for(int i = 0; i<31; i++)cout << (1 << i) << '\n';
	return 0;
}