#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
const int MOD = 1e9 + 7;


//dp[i][j] = dp[i-1][j-1] se v[i] != 0, dp[i-1][j-1]


int main() {
	int n, m;
	cin >> n >> m;
	vector<int> v(n);
	for (int i = 0; i < n; i++) cin >> v[i];
	for (int i = 0; i < n; i++) {
		for (int j = 1; j <= m; j++){
			dp[i]
		}
	}

	return 0;
}