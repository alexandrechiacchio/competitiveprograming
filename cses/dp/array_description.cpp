#include <bits/stdc++.h>
using namespace std;

const int N = 1e5+10;

vector<int> v(N);
vector<vector<int>> W(N, vector<int>(110));


int main(){
	int n, m;
	cin >> n >> m;
	for(int i = 0; i<n; i++) cin >> v[i];

	return 0;
}