#include <bits/stdc++.h>
using namespace std;

// um vetor pra coluna e outro pra linha

int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	vector<int> l(1e6+10), c(1e6+10);
	l[1] = 2
	for (int i = 1; i <= 1e6; i++){

	}
		int t;
	cin >> t;


	return 0;
}