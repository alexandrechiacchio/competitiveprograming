#include <bits/stdc++.h>
using namespace std;

const int N = 1e5+5;


int n, k, t, a[N];
pair<int, int> a[N];

int main(){
    scanf("%d", &t);
    while(t--){
        scanf("%d%d", &n, &k);
        for(int i = 0; i<n; i++)scanf("%d", &a[i]);
        
    }
    return 0;
}